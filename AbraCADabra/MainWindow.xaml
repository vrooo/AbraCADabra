<Window x:Class="AbraCADabra.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:otk="clr-namespace:OpenTK;assembly=OpenTK.GLControl"
        xmlns:local="clr-namespace:AbraCADabra"
        mc:Ignorable="d"
        Title="AbraCADabra" Height="600" Width="800" MinHeight="200" MinWidth="400"
        >

    <Window.Resources>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style TargetType="{x:Type Slider}">
            <Setter Property="Margin" Value="1,5,5,0"/>
            <EventSetter Event="ValueChanged" Handler="SliderRedraw"/>
        </Style>
        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="Margin" Value="0,5,0,0"/>
            <EventSetter Event="Checked" Handler="RadioRedraw"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Margin" Value="1,5,5,0"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <WindowsFormsHost x:Name="WFHost" Grid.Column="0">
            <otk:GLControl x:Name="GLMain"
                           Load="OnLoad" Paint="OnRender" Disposed="OnDisposed"
                           MouseMove="OnMouseMove" MouseWheel="OnMouseScroll"/>
        </WindowsFormsHost>
        <ScrollViewer Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <StackPanel Margin="3">
                <GroupBox Header="Torus">
                    <StackPanel>
                        <TextBlock>Big radius:</TextBlock>
                        <DockPanel>
                            <TextBox x:Name="TextBoxBigR" DockPanel.Dock="Right" Width="40" IsReadOnly="True"
                                     Text="{Binding ElementName=SliderBigR,Path=Value}"/>
                            <Slider x:Name="SliderBigR" DockPanel.Dock="Left"
                                    Minimum="1" Maximum="20" Value="5" TickFrequency="1" IsSnapToTickEnabled="True"/>
                        </DockPanel>
                        <TextBlock>Small radius:</TextBlock>
                        <DockPanel>
                            <TextBox x:Name="TextBoxSmallR" DockPanel.Dock="Right" Width="40" IsReadOnly="True"
                                     Text="{Binding ElementName=SliderSmallR,Path=Value}"/>
                            <Slider x:Name="SliderSmallR" DockPanel.Dock="Left"
                                    Minimum="1" Maximum="20" Value="5" TickFrequency="1" IsSnapToTickEnabled="True"/>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Wireframe">
                    <StackPanel>
                        <TextBlock>Vertical slices:</TextBlock>
                        <DockPanel>
                            <TextBox x:Name="TextBoxVerticalSlices" DockPanel.Dock="Right" Width="40" IsReadOnly="True"
                                     Text="{Binding ElementName=SliderVerticalSlices,Path=Value}"/>
                            <Slider x:Name="SliderVerticalSlices" DockPanel.Dock="Left"
                                    Minimum="3" Maximum="100" Value="10" TickFrequency="1" IsSnapToTickEnabled="True"/>
                        </DockPanel>
                        <TextBlock>Horizontal slices:</TextBlock>
                        <DockPanel>
                            <TextBox x:Name="TextBoxHorizontalSlices" DockPanel.Dock="Right" Width="40" IsReadOnly="True"
                                     Text="{Binding ElementName=SliderHorizontalSlices,Path=Value}"/>
                            <Slider x:Name="SliderHorizontalSlices" DockPanel.Dock="Left"
                                    Minimum="3" Maximum="100" Value="10" TickFrequency="1" IsSnapToTickEnabled="True"/>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
